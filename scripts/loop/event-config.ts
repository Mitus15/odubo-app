/**
 * The facts of the night, for the kit and the living poster. They now live in
 * src/lib/loop/poster/volumes.ts so the admin's studio reads the same block;
 * this module stays so the scripts' imports do not move.
 */
export { printedDate, VOLUMES, type VolumeDetails } from "../../src/lib/loop/poster/volumes";
