'use client';

/**
 * Database Tab - Database admin interface
 * Wrapper for the DB page component
 */
import AdminDbPage from '../db/page';

export default function DatabaseTab() {
  return <AdminDbPage />;
}
