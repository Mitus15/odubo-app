# Session log — 2026-08-11

Long session. Three distinct pieces of work: the video converter, the
facelessness decision, and the pre-code preview. Plus the dates got pinned down.

## 1. Video converter (shipped, PR #24)

Built `scripts/loop/video-convert.mjs` — two-pass, whole-scene. Replaces the old
Python converter. Full write-up and the tuning findings:
`docs/decisions/loop-video-converter.md`.

**The find that mattered:** every video in `media/` is already-rendered output
of the OLD GREEN filter. The only original unfiltered plate is
`digital-hub/loopsoulca/video-converter/IMG_0191.MOV`. That was not obvious and
cost time before it was spotted — recorded in memory so it isn't rediscovered.

**Deliverables** in `~/Documents/Loop-soul-the-entertainment-room/media/converted-2026-08/`:
- `loopsoul-dance-4k-20s.mp4` — 20s at 3840×2160 from the original plate
- `face-detail-comparison.png` — source vs output, which triggered decision 2
- `billie-jean-final-recolour-4k.mp4` — the final green render moved to the new
  palette (`mani-billie-jean-4.mov`, the last DaVinci export at 21:44 on Jul 9;
  the owner asked for only this one, not all four)

**Debugging note for next time:** three wrong diagnoses in a row came from
eyeballing ffmpeg crops. `-ss` seeking does NOT land on the same frame as the
converter's decode, so "the same frame index" from two commands is comparing
different moments. Use `--debug` / `--debugMask` and a contact sheet instead.

## 2. The Faceless (decision)

The converter renders heads as solid ink. The owner's call: **make it the
feature.** Full reasoning: `docs/decisions/loop-soul-the-faceless.md`.

- The planned face pass for the converter is **cancelled**.
- The app filter keeps its face pass but re-scoped to *form, not features*.
- Feeds the legal pass as a privacy control (not a substitute for consent).
- Shipped as product copy in the pre-code preview.

## 3. Pre-code preview (built, committed, not pushed)

Was the top blocker on announcing: in `live` phase a non-holder saw a bare code
prompt with no idea what a pass buys.

- New `src/components/loop/portal/PortalPreview.tsx`, rendered under the gate in
  `PortalHome`. Shows passes remaining, the programme, the anthem (with live
  nomination count and stage), Danceyokey (with live spot count), the camera,
  the Wall, and the "phone stays in your pocket" line.
- Nothing sensitive: the programme is what's on the poster, the anthem longlist
  is already public-safe. Participation stays gated.

**`LOOP_FORCE_PHASE`** added to `src/lib/loop/hub.ts`. Local dev runs against
the production database, and a phase was stored there, so previewing `live`
locally used to mean flipping the real one — the exact footgun that hit us
earlier. The new override outranks D1 but is **ignored in production builds**,
so it can't override an admin on the live site.

Verified at 375×812 with `LOOP_FORCE_PHASE=live`: gate renders, preview renders
under it with live data. Production D1 untouched — `event_phase` is still
`pre`, confirmed by read-only query.

## 4. Dates pinned

Rocky Mountaineer leaves Scott's **Tue 13 Oct**. Saturday before = **10 Oct**;
a week earlier = **hard cap Sat 3 Oct**. Working date stays **Sat 12 Sept**,
which leaves three spare weekends as slack. This corrects the earlier "on or
before Sept 29" estimate. Now tracked in
`docs/decisions/loop-soul-volume-1-worklist.md`.

## 5. Documents folder

`~/Documents/Loop-soul-the-entertainment-room/documents/` created with
`START-HERE.md` listing the six documents to draft and their status. The owner
wants to draft these **together**, not receive them — so only the index exists,
no fabricated content.

## Where to pick up

`docs/decisions/loop-soul-volume-1-worklist.md` is the running state. Next:
1. Push the pre-code preview branch and deploy it.
2. Draft the Scott's brief (owner + Claude, together) — longest external lead.
3. Teams, because captain recruitment depends on it existing.

Owner's parallel track: band + rehearsal day, team captains, crew, print order.
